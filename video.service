[Unit]
Description=video
After=network.target user.slice

[Service]
ExecStart=/home/pi/comtrola--navio-video-telemetria/video
ExecReload=/bin/kill -HUP $MAINPID
Restart=always

[Install]
WantedBy=multi-user.target
Alias=video.sevice
